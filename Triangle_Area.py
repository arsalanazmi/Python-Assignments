# TASK # 12
# COMPUTE THE AREA OF TRIANGLE BY TAKING BASE & HEIGHT AS USER INPUT

base = int(input("Enter Base of Triangle: "))
height = int(input("Enter Height of Triangle: "))

area = (1/2) * base * height
print("Area Of Triangle = 1/2 b.h = ",area)